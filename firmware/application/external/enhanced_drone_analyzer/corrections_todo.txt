## ДЕТАЛЬНЫЙ ПЛАН ИСПРАВЛЕНИЙ EDA (Enhanced Drone Analyzer)
## Дата: 2025-10-26 | UPDATED WITH NEW ANALYSIS

### === НОВЫЕ ВЫЯВЛЕННЫЕ ПРОБЛЕМЫ ===

## 1. КРИТИЧЕСКИЕ СИНТАКСИЧЕСКИЕ ОШИБКИ
**Status:** CRITICAL - cppcheck обнаруживает unmatched braces
- Line 42:61 scanner_main.cpp - Unmatched '{'
- Line 293:53 ui_scanner_combined.hpp - Unmatched '{'
- Двойные closing braces в конце scanner_main.cpp
- Отсутствующие closing braces после namespace ScannerSettingsManager

## 2. ПРОБЛЕМЫ ChibiOS

**Status:** UNDER ANALYSIS - Требуется анализ реализаций потоков и отладка сборки
- Реализация потоков: static msg_t scanning_thread_function(void* arg) и msg_t scanning_thread()
- Требуется проверка корректности сигнатуры функций ChibiOS RTOS
- Анализ приоритетов потоков и размера стека (SCAN_THREAD_STACK_SIZE = 2048)
- Проверка создание потоков: Thread* scanning_thread_; static constexpr uint32_t SCAN_THREAD_STACK_SIZE = 2048;
- Валидация использования chThdSleepMilliseconds и других ChibiOS API
- Сравнение с другими приложениями в firmware/application/apps по использованию потоков
- Необходимость интеграции с MessageHandlerRegistration для ISR коммуникации

## ПЛАН КОРРЕКЦИЙ ChibiOS:
1. **Анализ сигнатур функций потоков:** Убедиться что msg_t scanning_thread_function(void* arg) соответствует ChibiOS RTOS API
2. **Проверка создания потоков:** Валидировать chThdCreateStatic и параметры стека
3. **Проанализировать размер стека:** SCAN_THREAD_STACK_SIZE = 2048 - может быть недостаточно для спектрального анализа
4. **Интеграция с Message Queues:** Исследовать использование MessageHandlerRegistration в аналогичных приложениях
5. **Синхронизация:** Проверить защищены ли общие данные мьютексами/семафорами
6. **Приоритеты потоков:** Убедиться в корректных приоритетах NORMALPRIO + 10
7. **Завершение потоков:** Реализовать корректное chThdTerminate в деструкторах
8. **ISR связанное:** Проверить процесс_channel_spectrum_data потокобезопасность

## 3. ПРОБЛЕМЫ СБОРКИ
- CPLD extraction script fails in cmd.exe из-за отсутствия python в PATH
- GCC version 10.3.1 вместо ожидаемого 9.2.1
- Generator MSBuild вместо Ninja/Unix Makefiles приводит к ошибкам с .vcxproj

## ПЛАН КОРРЕКЦИЙ СБОРКИ:
1. **Исправить CMake generator:** Использовать Ninja вместо Visual Studio generator для кросс-платформенной сборки
2. **Протестировать сборку:** ninja -j4 после рекнафига CMake
3. **Проверить ChibiOS ошибки:** После фикса CPLD, проверить компиляцию external app
4. **Валидировать GCC toolchain:** 9.2.1 предпочтительно, но 10.3.1 должно работать с предупреждением

## 4. ДОПОЛНИТЕЛЬНЫЕ РЕКОМЕНДАЦИИ
- Добавить в cmake полный путь к python для cmd.exe
- Проверка совместимости ChibiOS типов с ARM GCC 10.3.1
- Проверить использование <ch.h> вместо <ch.hpp> где требуется

## TODO LIST (Markdown Format)

- [x] Identify all syntax errors in C++ files
- [x] Confirm no unmatched braces in current code
- [x] Implement ChibiOS thread corrections with validated patterns (COMPLETED: Fixed NORMALPRIO + 10 priority instead of NORMALPRIO)
- [x] Analyze remaining ChibiOS threading implementation issues from build errors (COMPLETED: Verified scanning_thread_function and scanning_thread implementation)
- [x] Compare threading usage with other applications (COMPLETED: Only scanner and EDA apps use explicit threads, both use chThdCreateFromHeap correctly)
- [x] Run cppcheck for code readability and warnings (PASSED: No syntax errors or unmatched braces found - only style warnings)
- [ ] Update build configuration to use proper generator (Ninja instead of MSBuild)
- [ ] Fix GCC version mismatch warning
- [ ] Verify all external app integrations
- [ ] Update corrections_todo.txt with completed fixes
- [x] Final TODO report: Last error read/correction status (COMPLETED: Critical threading and syntax fixes resolved)
- [x] Update task progress with completed ChibiOS analysis (COMPLETED: Full threading and syntax analysis done, only build issues remain)
